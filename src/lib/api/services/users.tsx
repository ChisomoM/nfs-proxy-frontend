import { list, post, remove, retrieve, update } from "../crud";

export const UsersService = {
    async updateProfile(firstName: string, lastName: string, mobile?: string) {
        const response = await update("UPDATE_MERCHANT_USER",)

        }
}